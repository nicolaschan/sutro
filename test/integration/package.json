{
  "name": "sunset-integration-tests",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test --test-concurrency=2 --test-timeout=120000 *.test.mjs"
  },
  "dependencies": {
    "puppeteer": "^24.0.0",
    "serve-handler": "^6.1.6"
  }
}
